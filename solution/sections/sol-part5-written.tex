% ============================================================
% SOLUTION — PART V: WRITTEN QUESTIONS
%
% AI INSTRUCTION:
% - Mirror the exam's question and sub-part numbering exactly
% - Each model answer must contain every element the exam demanded
%   ("Your answer must include: ...") --- check them off one by one
% - For computations: show every step, then state the conclusion in words
% - Use \begin{keypoint} for the formula or definition being tested
% - Use \begin{caution} where the grader expects to see students slip
% ============================================================

\section*{Part V: Written Questions --- Solution}

\subsection*{Question 1}

\textbf{(a)}
\begin{ans}
Model answer covering the three required elements:
\begin{itemize}
    \item \textbf{Role of each component:} ...
    \item \textbf{Order of the steps:} ...
    \item \textbf{Guaranteed property:} ...
\end{itemize}
\end{ans}

\textbf{(b)}
\begin{ans}
\textbf{Given:} $x = 15$, $y = 4$, $z = 12$.

\textbf{Compute:} show the substitution and the arithmetic.

\textbf{Conclusion:} state what the number means for the scenario ---
a number without an interpretation is not a complete answer.
\end{ans}

\begin{keypoint}[Formula]
$$ \text{the rule being applied} $$
\end{keypoint}

\textbf{(c)}
\begin{ans}
Explain the failure that occurs when the element is removed, and name the
attack or fault class it enables.
\end{ans}

\textbf{(d)}
\begin{ans}
\begin{itemize}
    \item \textbf{(i)} choice + one-sentence justification.
    \item \textbf{(ii)} choice + one-sentence justification.
\end{itemize}
\end{ans}

\vspace{4mm}

\subsection*{Question 2}

\textbf{(a)}
\begin{ans}
Full derivation, one step per line.
\end{ans}

\textbf{(b)}
\begin{ans}
Interpretation, plus the assumption that would invalidate the result.
\end{ans}

\vspace{4mm}

\subsection*{Question 3}

\textbf{(a)} \quad \textbf{(b)} \quad \textbf{(c)}
\begin{ans}
Model answers for the integrated question. Because this question spans two
topics, say explicitly where one topic hands off to the other.
\end{ans}
